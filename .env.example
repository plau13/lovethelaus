# Local dev only — Kitchen on port 3000 with base path /kitchen
PUBLIC_KITCHEN_URL=http://localhost:3000/kitchen

# Production builds use src/config.yaml (SITE.site) + npm run build:prod automatically.
# Override only if Kitchen runs elsewhere:
# PUBLIC_KITCHEN_URL=https://lovethelaus.com/kitchen
